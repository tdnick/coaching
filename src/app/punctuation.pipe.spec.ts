import { PunctuationPipe } from './punctuation.pipe';

describe('PunctuationPipe', () => {
  it('create an instance', () => {
    const pipe = new PunctuationPipe();
    expect(pipe).toBeTruthy();
  });
});
